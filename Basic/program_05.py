#
import numpy as np

a=np.arange(12).reshape(3,4)
print("Max value of this numpy array:",np.max(a),"\nMin value of this numpy array:",np.min(a),"\naddition of all data in this numpy array:",np.sum(a),"\nMultiply  of all data in this numpy array:",np.prod(a))